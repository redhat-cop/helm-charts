app_name: dev-ex-dashboard

# Docker deets
deployment:
  image:
    name: "quay.io/rht-labs/dev-ex-dashboard"
    tag: "latest"

# OCP route
route: true

# config - comment out as we want to control the config map separately