{{- if .Values.namespaces }}
{{- range $key := .Values.namespaces }}
{{ $ns:= printf "%s-%s" $.Values.prefix .name}}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $ns | quote}}
{{- end }}
{{- end }}
