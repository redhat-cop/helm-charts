{{- if .Values.global.serviceaccounts }}
{{- range $key := .Values.global.serviceaccounts }}
{{ $ns:= printf "%s" .namespace }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .name }}
  namespace: {{ $ns | quote }}
{{- end }}
{{- end }}
